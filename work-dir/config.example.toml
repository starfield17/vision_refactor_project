# The distributed runtime no longer uses a single global config.
# Keep this file as a pointer for operators who inspect work-dir first.
#
# Use these role-owned templates instead:
#   control_plane/config/config.example.toml
#   train/config/config.example.toml
#   autolabel/config/config.example.toml
#   edge_agent/config/config.example.toml
#   remote_worker/config/config.example.toml
#   stats_service/config/config.example.toml
#
# Runtime snapshots such as config.resolved.toml may still be written here by core
# jobs for reproducibility.
