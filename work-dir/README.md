# work-dir

本目录是运行时数据目录：

- `datasets/`
- `models/`
- `runs/`
- `outputs/`
- `stats/`
- `tmp/`

默认不提交运行产物。

使用方式：

1. 复制配置模板
   ```bash
   cp work-dir/config.example.toml work-dir/config.toml
   ```
2. 修改本地路径与参数
3. 运行 CLI
